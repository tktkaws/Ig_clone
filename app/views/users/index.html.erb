<div class="container">
  <div class="row">
    <% @users.each do |user| %>

      <div class="col-sm-6 col-md-4">
        <%= link_to image_tag(user.image.url, size: '300x300'), user_path(user.id) %>
      </div>

    <% end %>
  </div>
</div>
